//$(document).ready(function () {
    
//});

//字段值变化事件
function bindSurgerylogic(target, fieldkey, fielddata) {
    switch (fieldkey) {
        //case "t_radio.radiotherapycode":
        //    radiotherapycodeChanged(target, fielddata);
        //    break;
    }
}